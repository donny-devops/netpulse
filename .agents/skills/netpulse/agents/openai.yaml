interface:
  display_name: "Netpulse"
  short_description: "Repo-specific patterns and workflows for netpulse"
  default_prompt: "Use the netpulse repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true